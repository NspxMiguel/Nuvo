{
  "name": "nuvo",
  "version": "0.6.0",
  "description": "Nuvo — um servidor de casa que junta IAs locais, de API e de CLI numa interface só, com memória compartilhada entre todas elas.",
  "type": "module",
  "bin": {
    "nuvo": "bin/nuvo.mjs"
  },
  "scripts": {
    "start": "node bin/nuvo.mjs",
    "dev": "node --watch bin/nuvo.mjs",
    "test": "node --test --test-timeout=30000 test/*.test.mjs",
    "empacotar": "node build/landing-versao.mjs && node build/empacotar.mjs"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "license": "MIT",
  "private": true
}
