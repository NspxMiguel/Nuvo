{
  "name": "iaunifier",
  "version": "0.1.0",
  "description": "IA Unifier — um servidor de casa que junta IAs locais, de API e de CLI numa interface só, com memoria compartilhada entre todas elas.",
  "type": "module",
  "bin": {
    "iaunifier": "bin/iaunifier.mjs"
  },
  "scripts": {
    "start": "node bin/iaunifier.mjs",
    "dev": "node --watch bin/iaunifier.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "license": "MIT",
  "private": true
}
